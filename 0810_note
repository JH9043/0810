
StarUML
realization 추상
generalization 상속
aggregation 집합 관계
composition 부분-전체 관계
association 협력

cmd
jar cvf mylib.jar C:\JavaHome
jar xvf mylib.jar
 파일 만들때 cvf 사용 (기억해놓기)
 압축 풀 때 xvf 사용 (거의 풀일 없음)
 
 FQDN(Fully Qualified Domain Name) 전체경로

 
 1. package
 - 생성된 클래스나 인터페이스나 enum -> 참조형 타입객체들을 분류해서 저장하는 방법
 - javac -d <path> : package의 시작 폴더는 -d 옵션의 경로에 생성된다
 - 패키지가 상.하위로 구분되어 있다면 도트(.)를 사용하여 표현
 2. import
 -com을 찾는 위치
 1) 현재 디렉토리 
 2)Reference library에서 찾고
 3)classpath에서 찾고
 4)Class Not Found
 
 *접근제한자*
 바깥 클래스는 private, public만 사용 가능
 public 전체 공개
 protected 같은 패키지 또는, 패키지가 달라도 부모 자식 간에라면 접근 가능
 default 현재 패키지에서 접근 가능
 private 자기 클래스끼리만 접근 가능
private < default < protected < public


중첩 클래스(Nested Class) (헷갈리면 클래스 대신 변수 붙이기)
1. 생성법
- OuterClass.InnerClass 참조변수 = new OuterClass.InnerClass()
2. 제한 사항
- Static Inner Class는 멤버 변수/ 멤버 메소드에 접근할 수 없다
- 연결하려면 주소를 이용하여 연결 가능
- 멤버는 Static에 접근할 수 있다(이미 메모리에 올라와있기 때문에)
- Static은 주소 없이 클래스 이름으로 접근 가능

멤버 클래스(Member Class)
1. 생성법
- 바깥 클래스의 주소.new 안쪽클래스 생성자()
2. 제한 사항
- Member Inner Class는 static 변수와 static method를 가질 수 없다
- 상속될 경우에 부모 클래스까지도 접근 가능

로컬 클래스(Local Class) - 지역 변수하고 비슷하다고 생각하면 됨
1. 생성법
- 지역 클래스가 속해있는 메소드가 호출되어야 하고
- 해당 메소드 내에서 클래스 선언이 먼저, 그 다음 생성자로 생성
2. 제한 사항
- 접근 제한자(Access Modifier) 사용 불가
- 로컬 클래스는 멤버 클래스이기 때문에 static 사용 불가
- static 변수 / 메소드 가질 수 없다

익명 클래스(Anonymous Class)
- 이름이 없는 클래스
- 자식을 정의할 필요없이 기존의 클래스를 재정의한 클래스를 익명 클래스
 
 Comparable<E>
 - compareTo
 Comparator<T>
 - compare(A,B)
 
 enum 클래스(Enum Class)
 - 데이터 타입 생략 (int ,string 등)
- 주의할 점 
상수 제일 먼저 써줘야 됨
생성자 앞에 접근 제한자 못 씀

enum 왜 쓰냐?
Switch에서 써먹음
경계를 지정할 때 사용(enum이 가지고 있는 값과 같은지 확인)
 
 1. Class : 멤버 변수, 멤버 상수, static 변수, static 상수
 2. Interface : static 상수, public static final이 생략된 상수
 
 